export enum Environment {
  DEV = 'development',
  PROD = 'production',
}

export enum DateTimeFormat {
  Basic = 'yyyy-MM-dd HH:mm:ss',
  Pretty = 'MMM dd, yyyy - HH:mm:ss',
}

export enum DateFormat {
  Basic = 'yyyy-MM-dd',
}

export enum PubSubChannel {
  AccessTokenDeleted = 'AccessTokenDeleted',
}
